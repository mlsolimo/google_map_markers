import React from 'react';

const Home = () => {
    return(
        <div>
            <h1>Markers Application</h1>
            <p>It's an application for adding markers in a map, from google maps. 
            If you wish, you can delete a marker or see it in the map</p>
        </div>
    );
}

export default Home;